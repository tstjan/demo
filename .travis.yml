language: java
jdk:
  - openjdk8
sudo: false
script: mvn clean package -Dgpg.skip=true
after_success:
  - if [ "$JAVA_HOME" = "/home/travis/oraclejdk8" ]; then mvn test cobertura:cobertura coveralls:report; fi
